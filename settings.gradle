pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'mio-system'

include 'common'
include 'datacenter'
include 'cco-server'
include 'bus-simulator'
include 'web-gateway'
include 'experiments'